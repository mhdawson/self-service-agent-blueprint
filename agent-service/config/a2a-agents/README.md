Add agent cards to this directory to make them available to the
core agents

agent cards for a2a agents build as part of the quickstart will be
automatically copied into this directory when the agent service
is built
